import { ClosemenuDirective } from './closemenu.directive';

describe('ClosemenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosemenuDirective();
    expect(directive).toBeTruthy();
  });
});
